The folder names in the packages folder are the names of the nuget libs that can be downloaded as a nuget package by searching for the name, if for any reason the nuget restore does not work for you.

These packages are needed for MemorySharp to work.


Fasm.NET.1.70.03.2++
Newtonsoft.Json.7.0.1++